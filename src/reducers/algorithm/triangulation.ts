const run = () => {
    const steps = [];

    return steps;
}

export default {
    run,
    name: "Triangulation",
    parameters: {
    },
    dependencies: {
        nodes: ['position'],
        edges: [],
    },
    requirements: {
        directed: false,
    },
}
